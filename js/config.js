/* Trello API key for this Power-Up.
   Safe to ship in public client-side JS — Trello Power-Ups are designed to
   expose their app key this way (it identifies the app, it does not grant
   access by itself; the per-member token from the one-time "Authorize" step
   is what actually grants read access, and that token is never in this file).
   Generate/rotate at: https://trello.com/power-ups/6a6ad77437d17bf7584b0da7/api-key
   IMPORTANT: also add https://trello.transentertainment.id to "Allowed Origins"
   on that same page, or the authorize popup will be rejected by Trello. */
var TRELLO_APP_KEY = '7222e44f0b55d64e29c970502c3fe647';
var TRELLO_APP_NAME = 'Pipeline Monitor';
